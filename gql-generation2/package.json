{
  "name": "elm-worker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm install && npm run build",
    "build": "elm make src/Worker.elm --output=dist/elm.compiled.js",
    "go": "rm -rf ./output && npm run build && node index.js $1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "0.19.1-5"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
